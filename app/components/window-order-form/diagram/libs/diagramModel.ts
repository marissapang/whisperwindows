// app/components/window-order-form/libs/diagramModel.ts

// Placeholder for later refactor (Feature 1–3). Not used yet.
export type DiagramViewport = { svgWidth: number; svgHeight: number; pad: number; inset: number };
